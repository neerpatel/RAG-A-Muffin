# .NET build artifacts
bin/
obj/

# ASP.NET temporary files
*.db
*.log
*.user
*.suo
*.userosscache
*.sln.docstates

# Rider / IDE
.idea/
.vscode/
*.iml

# OS files
.DS_Store
Thumbs.db

# ── Sensitive / local config ──────────────────────────────────────────────────
# Google OAuth client secrets — never commit
credentials.json

# Per-developer overrides (connection strings, local ports, etc.)
appsettings.Development.json
appsettings.local.json
appsettings.*.local.json

# NOTE: appsettings.json IS committed (base config) but contains Ingestion:UserId.
# If this repo becomes public, move UserId to appsettings.local.json instead.

# OAuth tokens written at runtime
data/
**/data/tokens/

# ── Docker ────────────────────────────────────────────────────────────────────
**/docker-compose.override.yml
**/docker-compose.*.override.yml

# ── NuGet ─────────────────────────────────────────────────────────────────────
*.nupkg
packages/
.nuget/

# ── IDE / tool caches ─────────────────────────────────────────────────────────
*.cache
*.lscache
*.vs/
.vs/

# ── Generated files ───────────────────────────────────────────────────────────
*.g.cs
*.g.i.cs
*.generated.cs
*.designer.cs
*Generated.*
**/Generated/
*.swagger.json

# ── Planning docs / local tooling ────────────────────────────────────────────
PLAN.md
.claude/

# ── Temp / scratch ────────────────────────────────────────────────────────────
*.tmp
*.temp
*~
*.pid
*.seed

# ── Visual Studio / Rider user-specific ──────────────────────────────────────
*.psess
*.vspx
*.userprefs
